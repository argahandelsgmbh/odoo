<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="so_to_ticket_wizard_form" model="ir.ui.view">
            <field name="name">so.to.ticket.wizard.form</field>
            <field name="model">quote.ticket.wizard</field>
            <field name="arch" type="xml">
                <form string="Quote to ticket">
                    <sheet>
                        <group col="4">
                            <field name="sale_id" readonly="1"/>
                            <field name="partner_id" required="1"/>
                        </group>
                        <notebook>
                            <page string="Products">
                                <field name="sale_line_ids" domain="[('order_id', '=', sale_id)]">
                                    <list create="0">
                                        <field name="product_id"/>
                                        <field name="product_uom_qty"/>
                                        <field name="product_uom_id"/>
                                        <field name="price_unit"/>
                                        <field name="tax_ids" widget="many2many_tags"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                    <footer>
                        <button name="create_ticket" type="object" string="Create Ticket" class="btn-primary"/>
                        <button string="Cancel" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_so_to_ticket_wizard" model="ir.actions.act_window">
            <field name="name">Quote to Ticket</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">quote.ticket.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="context">{'default_sale_id': active_id}</field>
        </record>

    </data>
</odoo>